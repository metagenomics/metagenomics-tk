name: "Publish Documentation"
on:
  push:
    branches: [ dev, master ]

jobs:
  build_job:
    runs-on: [self-hosted, html]
    steps:
      - name: "Publish wiki"
        run: |
           make publish_wiki_html BRANCH=${{ github.base_ref }}
