tempdir: "tmp"
summary: true
input: "test_data/fullPipeline/reads_split_s3.tsv"
output: "output"
runid: 1
steps:
  qc:
    download:
      s5cmdParams: " --retry-count 30 --no-verify-ssl --no-sign-request --endpoint-url https://openstack.cebitec.uni-bielefeld.de:8080 "
    interleaved: false
    fastp:
