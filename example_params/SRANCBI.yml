tempdir: "tmp"
s3SignIn: true
output: "output"
logDir: log
runid: 1
scratch: "/vol/scratch"
publishDirMode: "symlink"
input:
  SRA:
    pattern:
      ont: ".+[^(_1|_2)].+fastq.gz$"
      illumina: ".+(_1|_2).+fastq.gz$"
    NCBI:
      sheet: test_data/SRA/ncbi_samples.tsv 
steps:
  empty:
