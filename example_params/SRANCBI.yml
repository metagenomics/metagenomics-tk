tempdir: "tmp"
summary: false
s3SignIn: true
output: "output"
logDir: log
runid: 1
scratch: "/vol/scratch"
publishDirMode: "symlink"
input:
  SRA:
    pattern:
      ont: ".+[^(_1|_2)].+$"
      illumina: ".+(_1|_2).+$"
    NCBI:
      path: test_data/SRA/ncbi_samples.tsv 
steps:
